# Paper Workspace

`JMLR_MANUSCRIPT_SKELETON.md` fixes the argument and section ownership before
protected evidence arrives. Regenerate current evidence tables with:

```bash
python paper/build_tables.py
```

Generated Markdown tables are tracked under `paper/tables/`. They are derived
only from committed ledgers and the machine-readable claim registry.
